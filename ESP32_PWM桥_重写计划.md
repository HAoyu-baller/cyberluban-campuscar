# ESP32 PWM 桥文档重写计划

> **供智能代理执行：** 必须使用 `superpowers:executing-plans`，按照任务顺序逐项实施本计划。

**目标：** 将 `ESP32.md` 重写为一份经过验证且安全优先的操作指南，指导团队使用由 Mac 或 NUC 控制的 ESP32 PWM 桥替代 HOTRC F-06A 接收机。

**系统架构：** Mac 或 NUC 通过 USB 串口，以 115200 波特率向 ESP32 发送按行分隔的控制命令。ESP32 解析并验证命令，然后输出两路与 F-06A 接收机兼容的独立 50 Hz RC PWM：CH1 用于转向，CH2 用于油门。上电中立保持和通信超时保护都会强制输出 1500 µs 中立脉宽。

**技术栈：** Arduino IDE 2.x、Espressif Arduino Core、ESP32Servo、USB 串口、兼容 HOTRC DS600/F-06A 的 RC PWM。

## 全局约束

- PWM 频率：50 Hz，周期 20 ms。
- 默认端点范围：1000～2000 µs。
- 默认中立脉宽：1500 µs。
- GPIO25 对应 CH1 转向；GPIO26 对应 CH2 油门。
- ESP32 与小车控制器必须共地；ESP32 已由 USB 供电时，不得再连接控制器接收机接口的 VCC。
- ESP32 上电后必须保持中立输出 3 秒，之后才能接受运动命令。
- 收到格式错误的命令，或连续 500 ms 未收到有效运动命令时，必须停车并回到中立输出。
- 初次测试必须架空驱动轮，现场必须有人能够立即断电。

---

### 任务 1：修正串口协议与 ESP32 固件

**文件：**
- 修改：`ESP32.md`

**接口：**
- 输入：`S <speed> <steering>\n`、`STOP\n`、`STATUS\n`
- 输出：GPIO25/GPIO26 上的 50 Hz PWM，以及 `OK`、`ERR`、`STATE` 串口回执

- [ ] 替换当前错误的解析代码；原代码会错误地要求 `S300 0` 中存在第二个空格。
- [ ] 将运动命令统一定义为 `S <speed> <steering>`，三个字段之间由两个空格分隔，例如 `S 300 0`。
- [ ] 将速度和转向输入强制限制在 `[-1000, 1000]`。
- [ ] 增加上电保持中立 3 秒的解锁阶段和 500 ms 通信超时停车保护。
- [ ] 增加可配置的油门/转向反向选项，以及明确的 PWM 最小值、中位值和最大值。
- [ ] 增加串口回执，让操作者能够确认 ESP32 是否真正解析并执行了命令。

### 任务 2：重写接线与环境配置说明

**文件：**
- 修改：`ESP32.md`

- [ ] 明确记录 GPIO25 接替 F-06A 的 CH1 转向信号、GPIO26 接替 CH2 油门信号，并连接公共 GND。
- [ ] 解释 ESP32 由 USB 供电时，为什么必须断开原接收机接口的 VCC，仅连接信号线和 GND。
- [ ] 补充 Windows/macOS 下 Arduino IDE 安装、ESP32 开发板包安装、ESP32Servo 库安装、串口选择及按 BOOT 键烧录的完整流程。

### 任务 3：增加分阶段验证与校准流程

**文件：**
- 修改：`ESP32.md`

- [ ] 在连接小车控制器之前，先通过串口回执验证命令解析是否正确。
- [ ] 如果具备示波器或逻辑分析仪，验证 PWM 是否为 50 Hz，并验证中立、最小和最大脉宽。
- [ ] 架空驱动轮，只上电并保持中立，验证控制器是否能正常解锁且车轮不动。
- [ ] 首先测试 `±100` 的小行程命令，确认正确后再逐步测试 `±300` 或更大命令。
- [ ] 增加 CH1/CH2 接反、方向反向、3.3 V 逻辑电平不足、控制器未解锁和超时保护异常等问题的排查流程。

### 任务 4：明确 PPT 串口协议与 RC PWM 的边界

**文件：**
- 修改：`ESP32.md`

- [ ] 解释 PPT 中的 `0xABCD` 帧描述的是 NUC 与控制器之间的二进制串口通信层，而替代 HOTRC 接收机的方案使用物理层 RC PWM，两者不是同一种协议。
- [ ] 说明 ESP32 PWM 桥第一版故意使用便于人工检查的文本串口命令，以便安全调试和快速定位问题。
- [ ] 定义后续 ROS2 适配层边界：`/cmd_vel` → 经过验证的串口命令 → ESP32 PWM。

### 验证清单

- [ ] 在 Arduino IDE 中选择 ESP32 Dev Module，固件能够成功编译并烧录。
- [ ] 发送 `STATUS`，ESP32 返回一条 `STATE ...` 状态信息。
- [ ] 以 20 Hz 连续发送 `S 100 0`，测得 CH2 约为 1550 µs，CH1 约为 1500 µs。
- [ ] 停止发送命令超过 500 ms，两个通道都自动恢复到 1500 µs。
- [ ] 发送格式错误的命令，ESP32 返回 `ERR`，并保持两个通道为中立输出。
- [ ] 架空驱动轮，验证小行程运动和 `STOP` 停车均正常后，才能进行落地测试。
